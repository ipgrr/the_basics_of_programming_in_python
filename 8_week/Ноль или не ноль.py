print(
    any(
        map(
            lambda x: x == 0,
            list(
                map(
                    lambda x: int(input()),
                    range(
                        int(input()
                            )
                          )
                )
            )
        )
    )
)
